# Remotely Docker Deployment Configuration
# Copy this file to .env and modify the values as needed

# Basic Configuration
PORT=5000
DOMAIN=localhost

# Database Configuration
# Options: SQLite, SQLServer, PostgreSQL
DB_PROVIDER=SQLite

# SQLite (default)
# Uses: Data Source=/app/AppData/Remotely.db

# SQL Server (uncomment to use)
# SQL_SERVER_CONNECTION=Server=localhost;Database=Remotely;Trusted_Connection=True;MultipleActiveResultSets=true

# PostgreSQL (uncomment to use)
# POSTGRESQL_CONNECTION=Host=localhost;Database=Remotely;Username=postgres;Password=changeme;

# Security Settings
FORCE_HTTPS=false
USE_HSTS=true
REDIRECT_HTTPS=true

# SMTP Configuration (optional)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@yourdomain.com

# Traefik Configuration (for automatic HTTPS)
# Uncomment these if using Traefik with Let's Encrypt
# ACME_EMAIL=admin@yourdomain.com
# ACME_CA_SERVER=https://acme-v02.api.letsencrypt.org/directory

# Development/Staging (use staging Let's Encrypt)
# ACME_CA_SERVER=https://acme-staging-v02.api.letsencrypt.org/directory

# Application-specific Settings
# MAX_ORGANIZATION_COUNT=-1
# REQUIRE_2FA=false
# ENFORCE_ATTENDED_ACCESS=false
# ENABLE_REMOTE_CONTROL_RECORDING=false
# DATA_RETENTION_DAYS=90
# THEME=Light